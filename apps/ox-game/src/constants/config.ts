export const GAME_SCENE_KEY = Object.freeze({
  BOOT: "Boot",
  PRELOAD: "Preload",
  LOGIN: "Login",
  LOBBY: "Lobby",
  READY: "Ready",
  GAME: "Game",
});

export const FIREBASE = Object.freeze({
  userCredential: "userCredential",
});

export const LAYOUT_SIZE = Object.freeze({
  width: 1280,
  height: 1920,
});
